import {} from './PFANS6008Api'
const PFANS6008Store = {
  namespaced: true,
  state: {},
  mutations: {},
  actions: {
    // getvariousfunds() {
    //   return new Promise((resolve, reject) => {
    //     getvariousfunds().then(response => {
    //       if (response.code === 0) {
    //         resolve(response.data);
    //       } else {
    //         reject(response.message)
    //       }
    //     }).catch(error => {
    //       reject(error);
    //     })
    //   })
    // }
};

export default PFANS6008Store;
